# ouchi_pantry_privacy_policy

このリポジトリには以下のページが含まれています。

## `index.html`
OuchiPantry（食品在庫管理アプリ）のプライバシーポリシーページ。

## `quiz/`
国道が日本のどこからどこまで続いているかを、地図上で指（マウス）でなぞって覚えるクイズアプリです。
ビルド不要の静的HTML/JS/CSSのみで動作し、`quiz/index.html` をブラウザで開くだけで利用できます。

- **学習モード**：国道を選ぶと、実際のルートを地図上にアニメーション表示。通過する都道府県やトリビアも確認できます。
- **クイズモード**：出題された国道の起点から、指（マウス）で実際の経路をなぞって解答します。なぞった経路と実際のルートの一致度で採点し、正誤を判定します。ヒントで終点だけ先に確認することも可能です。
- 収録国道や成績はすべて端末内（`localStorage`）にのみ保存され、外部サーバーへの送信は行いません。

### 国道データの追加方法
`quiz/highways-data.js` の `HIGHWAYS` 配列にオブジェクトを1つ追加するだけで、新しい国道をアプリに追加できます（アプリ本体のコード変更は不要です）。現在は主要な国道12路線を収録していますが、今後のバージョンアップで追加していく想定の設計になっています。

```js
{
  id: 999,
  number: "国道◯号",
  startLabel: "起点の都市名",
  endLabel: "終点の都市名",
  prefectures: ["通過する都道府県", "..."],
  difficulty: 1, // 1(易) 〜 3(難)
  fact: "学習モードで表示する豆知識",
  waypoints: [
    [緯度, 経度, "地名"],
    // ... 経由都市を3〜8個程度
  ]
}
```

なお、ルートは実際の道路線形そのものではなく、主要都市を結んだ学習用の簡略ルートです。
